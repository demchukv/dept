import { domainType } from '@/types/domain';

interface domainTransferProps {
  data: domainType;
}
export const DomainTransfer = ({ data }: domainTransferProps) => {
  return <div>DomainTransfer</div>;
};
