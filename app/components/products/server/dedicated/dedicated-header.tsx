import { ServerType, DedicatedType } from '@/types/server';

interface DedicatedHeaderProps {
  data: ServerType;
}
export const DedicatedHeader = ({ data }: DedicatedHeaderProps) => {
  return <div>DedicatedHeader</div>;
};
