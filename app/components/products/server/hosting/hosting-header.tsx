import { ServerType, HostingType } from '@/types/server';

interface HostingHeaderProps {
  data: ServerType;
}
export const HostingHeader = ({ data }: HostingHeaderProps) => {
  return <div>HostingHeader</div>;
};
