import { subscriptionType } from '@/types/subscription';

interface SubscriptionTvInfoProps {
  data: subscriptionType;
}
export const SubscriptionTvInfo = ({ data }: SubscriptionTvInfoProps) => {
  return <div>SubscriptionTvInfo</div>;
};
