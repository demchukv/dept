import React from 'react';

interface RoleSettingsProps {
  settingsData: any;
  roleSettingsSchema: any;
  accessList: any;
  accessListBoolean: boolean[];
}
export const RoleSettingsDesktop = ({
  settingsData,
  roleSettingsSchema,
  accessList,
  accessListBoolean,
}: RoleSettingsProps) => {
  return <div>RoleSettingsDesktop</div>;
};
